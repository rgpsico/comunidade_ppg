FROM node:20-alpine AS build

WORKDIR /app
COPY cria-da-comunidade/package*.json ./
RUN npm ci
COPY cria-da-comunidade/ .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY docker/nginx/frontend.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
